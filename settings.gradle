pluginManagement {
    plugins {
        id "org.jetbrains.kotlin.plugin.lombok" version '1.9.25'
    }
}
rootProject.name = 'zola'

include(
        "auth",
        "gateway",
        "chat",
        "chatws",
        "chatwsrouter",
        "user"
)